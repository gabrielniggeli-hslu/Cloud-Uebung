<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Website</title>

    <!-- Space for CSS files-->
  </head>
  <body>
    <!-- Body content -->

    <!-- Space for JS files-->
  </body>
</html>
